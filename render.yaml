services:
  - type: web
    name: family-movie-recs
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn app:app"
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: TMDB_API_KEY
        sync: false
      - key: DATABASE_URL
        fromDatabase:
          name: family-movie-recs-db
          property: connectionString

databases:
  - name: family-movie-recs-db
    plan: free
